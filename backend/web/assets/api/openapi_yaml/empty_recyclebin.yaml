delete:
  operationId: empty recycle bin
  description: removes all files and folders in the recycle bin permanently
  responses:
    '200':
      description: >-
        a boolean indicating whether or not the deleted files and folders have
        been purged succesfully
      content:
        application/json:
          schema:
            type: object
            properties:
              error:
                type: boolean
