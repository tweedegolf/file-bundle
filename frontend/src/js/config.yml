api:
  openFolder: /list # {id: string}
  uploadFiles: /upload # {fileList: File[], parentFolderId: string}
  moveItems: /move # {fileIds: string[], folderIds: string []}
  deleteFile: /delete # {id :string}
  deleteFolder: /delete/folder # {id: string}
  addFolder: /create/folder # {name: string, parentFolderId: string}
  renameFolder: /rename/folder # {id: string}
  getMetaData: /metadata  # {fileIds: string[], folderIds: string []}
  getRecycleBin: /recycle_bin
  emptyRecycleBin: /recycle_bin/empty
